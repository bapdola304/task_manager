<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<meta http-equiv="X-UA-Compatible" content="ie=edge">
		<link rel="stylesheet" href="../style.css">
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
		<title>Login</title>
	</head>
	<body>
		<% include header%>
		<div class="container">
			<form class="form-signin" method="post" action="/user/postRegister">
				<% if(errors.length > 0){ 

					errors.forEach(err =>{
				%>
					<div class="alert alert-danger"><%= err %></div>
				<% }) } %>
				<h2 class="form-signin-heading">Please sign in</h2>
				<label for="inputPassword" class="sr-only">Full Name</label>
				<input type="text" class="form-control" placeholder="Name" name="name" value="<%= value ? value.name : '' %>">
				<label for="inputEmail" class="sr-only">Email</label>
				<input type="email" class="form-control" placeholder="Email address" name="email" value="<%= value ? value.email : '' %>" >
				<label for="inputPassword" class="sr-only">Password</label>
				<input type="password" class="form-control" placeholder="Password" name="password">
				<label for="inputPassword" class="sr-only">Password Again</label>
				<input type="password" class="form-control" placeholder="Password Again" name="passwordA">
				
				<button class="btn btn-lg btn-primary btn-block" type="submit">Submit</button>
			</form>
			</div> <!-- /container -->
			
			
			
			<footer class="footer">
				<p>&copy; 2016 Company, Inc.</p>
			</footer>
		</body>
	</html>